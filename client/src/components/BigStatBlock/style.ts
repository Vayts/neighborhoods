import styled from 'styled-components';

export const BigStatBlockWrapper = styled.div`
	padding: 15px;
	border-radius: 15px;
	background-color: #fff;
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.05);
`;

export const BigStatBlockTitle = styled.h2`
  font-weight: 500;
  font-size: 18px;
  align-self: flex-start;
  margin: 0 0 10px;
`;
