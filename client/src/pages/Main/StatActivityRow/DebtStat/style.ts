import styled from 'styled-components';

export const DebtStatWrapper = styled.div`

`;

export const DebtStatValue = styled.p`
	margin: 0;
	font-size: 30px;
`;

export const DebtStatSubTitle = styled.h6`
	color: ${({ theme }) => theme.subTxtColor};
	font-weight: 400;
	margin: 0;
	font-size: 20px;
`;
